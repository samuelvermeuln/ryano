# 2026-09-23

## Varredura completa do sistema (sessao agentes/skills)

Inventario: 53 paginas, 69 rotas API, 49 models, 32 enums, 6 layouts.

### Achados
1. `/escola/[schoolId]/turmas` — 404. Pasta existe VAZIA, menu lateral linka.
   Unico link quebrado do sistema. `/professor/[schoolId]/turmas` existe.
2. `/app/relatorios` nao e tela — redirect para `/app/perfil#notificacoes`.
3. Dois caminhos de landing com regras divergentes:
   - `resolveSmartLandingPath()` (server/auth-guards.ts, usado em /entrar):
     manda para `/escola` SEM id, nao checa vinculo de atleta, trata professor.
   - guard em `/app/dashboard`: manda para `/escola/{id}`, CHECA vinculo de
     atleta, nao trata professor.
4. **Next dev NAO emite 307 em redirect de Server Component**: responde HTTP 200
   com `<meta id="__next-page-redirect" http-equiv="refresh" url=...>`.
   `curl -w "%{redirect_url}"` mostra vazio → causou diagnostico errado nesta sessao.
   Detectar: `curl -s -b ck.txt URL | grep -oE '__next-page-redirect[^>]*url=[^"]*'`
5. Paginas publicas legitimas (8): `/`, `/entrar`, `/entrar/convite/[token]`,
   `/cadastro`, `/recuperar-senha`, `/redefinir-senha`, `/privacidade`, `/termos`.
   `/onboarding` usa `requireUserRecord()` — parece publica mas nao e.

### Matriz de acesso (medida por HTTP real, nao por leitura de codigo)
- Isolamento entre areas OK: nenhum perfil alcanca area de outro.
- `/app/*` compartilhado por desenho (espaco pessoal de qualquer usuario onboarded).
- `/escola` e `/professor` sem id sao dispatchers (sempre 307).
- Guards vivem no `layout.tsx` de cada area — a area e a fronteira de seguranca.

### Artefatos criados (commit 6127d2c)
- `.agents/agents/ryvano-{atleta,professor,escola,admin,auth}.md`
- `.agents/skills/ryvano-telas/` — SKILL.md + references/{screen-map,access-matrix}.md
  + scripts/{audit-routes,audit-access}.sh

Decisao: agente por PERFIL, nao por tela. 53 agentes ficariam obsoletos no
primeiro refactor; telas so fazem sentido dentro do perfil que as consome.

### Comandos uteis
- Auditoria estatica: `bash .agents/skills/ryvano-telas/scripts/audit-routes.sh`
- Matriz de acesso: `bash .agents/skills/ryvano-telas/scripts/audit-access.sh`
  (exige `pnpm dev` + seed e2e; contas `{owner.alpha,prof.carlos,aluno.joao}@ryvano-e2e.test` / `Teste123!`)

---

## Spec Jornada Escola v1.1 (commit c14b255)

`docs/RYVANO_JORNADA_ESCOLA_SPEC_V1.md` expandida de 321 para 1064 linhas.

### Defeitos VERIFICADOS no codigo (base 998c5bf) — viram a Onda 0 / T500-T519
- **D1** `/escola/[schoolId]/turmas` = 404. Pagina nao existe, mas ha 2 links vivos:
  `app/escola/[schoolId]/layout.tsx:44` (menu) e `page.tsx:134` (card "Turmas ativas").
  ATENCAO: `app/professor/[schoolId]/turmas/page.tsx` EXISTE — a lacuna e so na visao admin.
- **D2** `modules/school/application/manage-team.ts` tem 6 use cases prontos
  (CreateTeam, ArchiveTeam, Add/RemoveAthleteToTeam, Add/RemoveCoachFromTeam)
  mas NAO existe `app/api/schools/[id]/teams/` — zero rotas HTTP.
- **D3** `Team` (schema:871-884) so tem id/schoolId/name/archivedAt/timestamps.
- **D4** Card de solicitacoes conta so `schoolAthleteMembership` (page.tsx:61),
  mas a tela lista atletas (solicitacoes:73) + coaches (solicitacoes:78).
- **D5** Atrasados: `take: 200` (page.tsx:70) + `startOfUtcDay` (page.tsx:23-27).
- **D6** Layout persiste em `UserProfile.dashboardLayoutOrder` (schema:558,
  dashboard-layout.ts:39,43) — chave unica por usuario, sem escopo de escola.
- **D7** `CustomizableCardGrid` so opera por pointerdown; sem caminho por teclado.

### Convencoes confirmadas para rotas novas
- Envelope: `schoolResponse(op, status)` em `app/api/schools/_shared.ts`
  (ja trata SchoolError, ZodError -> 400, flag off -> 404).
- Erros: catalogo `SCHOOL_ERROR_STATUS` em `modules/school/domain/errors.ts`.
- Paginacao: `limit` 1..100 + `cursor` min1 max2048 (ver `lobby/route.ts`).
- `export const runtime = "nodejs"` + `dynamic = "force-dynamic"`.
- `SchoolRole`: OWNER, ADMIN, COACH, ASSISTANT_COACH, STAFF, ATHLETE, GUARDIAN.

### Numeracao para trabalho novo
- Proxima migration: **0035** (ultima e 0034_workout_requests).
- Proximo bloco de tasks: **T500+** (maior ID em uso e T407).

### Licao: DDL de spec precisa ser validado de verdade
Extrair os blocos prisma do markdown, concatenar ao schema real e rodar
`npx prisma validate`. Isso revelou que faltavam TODAS as back-relations
(User/School/CoachProfile/WorkoutAssignment) — a spec nao compilaria como
estava escrita. `User` participa de 8 relacoes com o dominio de jornada e
precisa de `@relation` nomeado em 4 pares (atleta vs proponente de
AthleteJourney; sujeito vs resolvedor de JourneyAttentionItem).
Cuidado tambem: `@@unique` com coluna nullable nao colide em Postgres —
decidir indice parcial ou sentinela NA migration.
