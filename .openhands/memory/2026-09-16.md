# 2026-09-16

## Onda 0 Jornada Escola — turmas (T500–T509, T513) — commit 98934d0

### Armadilha: `prisma format` polui o diff
Editar `prisma/schema.prisma` e rodar `prisma format` / `prisma generate`
realinha o arquivo INTEIRO — 352 linhas de ruído para uma mudança de 9.
Correção: `git --no-pager diff -w` mostra a mudança real; se o resto for só
espaçamento, `git checkout` do arquivo e reaplicar a edição à mão preservando
o alinhamento de colunas do model. Conferir `diff --stat` antes de commitar.

### Dev server: `npm run dev` reaproveita servidor existente
Não falha nem sobe outro; imprime "You can access the existing server".
O servidor morre ao compilar muitas rotas novas de uma vez (curl devolve
`000`). Reiniciar e dar ~6 s entre requisições a rotas ainda não compiladas.

### Verificação de rota por HTTP (padrão do repo)
Rota protegida existente responde 307 para /entrar; rota inexistente responde
404. Portanto "não responde 404" é o critério de existência — comparar sempre
com uma rota de controle inexistente. Rotas de API sem sessão respondem 401.

### `_count` do Prisma em listagens
`select: { _count: { select: { members: true, coaches: true } } }` traz
ocupação sem carregar os vínculos. Não vazar `_count` para fora do caso de uso.

### PATCH parcial: undefined != null
`UpdateTeam` usa `z.nullish()` e filtra `v !== undefined` antes do update.
Ausente = não mexer, `null` = limpar. Sem isso um PATCH de nome apaga capacity.

### Teste de mutação como validação do próprio teste
Antes de marcar `[x]`, remover temporariamente a regra (`>=` vira `>`, ou
apagar a linha de autorização) e confirmar que o teste FALHA. Isso comprovou
os casos de borda de capacidade e a proteção contra vazamento entre escolas.

### GitNexus segue expirando (exit 143)
`detect-changes --scope all` estoura mesmo com timeout de 180 s — segunda
sessão seguida. Substituto: grep dos consumidores do símbolo e declarar a
limitação no commit.

### check-status.sh é o guarda de consistência
`bash .kiro/specs/ryvano-jornada-escola/check-status.sh` compara task-list.md
com STATUS.md e FALHA se divergirem. Rodar antes de commitar — pegou T509 não
marcada e o "Total geral" desatualizado. STATUS.md tem contadores em DOIS
lugares: o bloco `Progresso: N/20` e `**Total geral: N/49**`.

### Divergência registrada: expectedVersion em Team
task-list T503/T506 pede `expectedVersion`, mas `Team` não tem coluna `version`
e a D-04 do design.md só prevê isso para AthleteJourney/JourneyMilestone.
Não implementado, por decisão, com nota justificando no task-list.

## Pull de origin/main — HEAD 364a9e3
`git pull --ff-only` limpo (fast-forward, árvore já estava limpa, sem conflitos).
ORIG_HEAD era b6017cc. 194 arquivos, +21599/-7385.
Commits novos: `1ecafb6 marketplace`, `aa36bf4 layout escola`,
`fbd4575 remove cache`, `364a9e3 fix build`.

### Mudança de gerenciador de pacotes (durável)
`fbd4575` DELETOU `pnpm-lock.yaml`. O projeto agora usa **npm** com
`package-lock.json` + `.npmrc` (`legacy-peer-deps=true`).
Não rodar `pnpm install` — recriaria o lockfile deletado.

### Build Docker sem cache (decisão deliberada, justificada no próprio diff)
`docker-compose.yml` ganhou `no_cache: true`; o Dockerfile perdeu os
`--mount=type=cache` de `/root/.npm` e `/app/.next/cache`. Motivo declarado:
cache persistente fazia produção servir build obsoleto. Também trocou
`npm ci` por `npm install --legacy-peer-deps` porque o `ci` falhava no
container (picomatch@2.3.2 vs ^4.0.7 — multi-versão real, não lockfile velho).
Não reintroduzir cache sem estratégia verificada.

### Pendências de ambiente criadas por este pull
- `stripe@^22.6.2` entrou em `package.json` mas não está em `node_modules`:
  `npm install` é necessário antes de rodar testes/tsc.
- 9 migrations novas não aplicadas: 0036..0044 (campos comerciais de
  TrainingProduct, imutabilidade de version, idempotência de checkout,
  ativação de licença, proveniência marketplace em WorkoutAssignment,
  coach engagement + plan adaptation, media/review, layout/orders em
  UserProfile, seller account + ledger). `schema.prisma` +364, `seed.ts` +109.

## Telas escola: atletas/turmas/solicitacoes/convites/marketplace (2026-09-16)

### Bug real: formatMoney atravessando a fronteira client/server
`marketplace/page.tsx` (Server Component) importava `formatMoney` de
`products-panel.tsx`, que tem `"use client"`. Resultado: HTTP **500** em
runtime ("Attempted to call formatMoney() from the server").
- `tsc --noEmit` passa. `vitest` passa. So aparece requisitando a rota.
- Correcao: mover para `modules/school/presentation/format.ts` (sem
  diretiva, serve os dois lados) - convencao do projeto.
- **Licao**: typecheck+testes nao cobrem a fronteira client/server.
  Rodar `npm run build` ou requisitar a rota antes de dar por pronto.

### Lint: react-hooks/purity proibe Date.now() no corpo do componente
`Date.now()` direto em Server/Client Component = **erro**, nao warning.
Mover para helper em nivel de modulo (`isIdle`, `isUsable`,
`expiresWithinAWeek`).

### Verificacao por HTTP (o que funcionou)
- `bash .agents/skills/ryvano-telas/scripts/audit-access.sh` - matriz
  perfil x rota. **Pegou o 500 do marketplace** que tsc/vitest nao pegaram.
  As 5 telas: 200 para owner, 307 para professor e atleta.
- Login e2e: `POST /api/e2e/login` com `owner.alpha@ryvano-e2e.test` /
  `Teste123!`. Escola de seed: `cmue80z3v003kod8dpb6yt51b`.
- `audit-routes.sh` sem pasta vazia nem link quebrado; `/escola/{id}/turmas`
  (404 historico anotado na skill) agora resolve.

### Armadilha: script tsx standalone nao conecta no banco
`npx tsx scripts/...` contra o Postgres remoto fica pendurado e morre por
timeout; o pool do `next dev` funciona. Validar mutacao pela rota HTTP.

### Invocar Server Action por curl: NAO REPETIR
ID em `.next/dev/server/app/<rota>/page/server-reference-manifest.json`
(chave `node`), header `Next-Action: <id>`, campo 0 = `[{},"$K1"]` para a
assinatura `(prevState, formData)` do `useActionState`. Cheguei a executar
a action (o erro Zod volta como state tipado), mas nao acertei o prefixo
dos campos: `decodeReply` usa `_prefix + "_" + id + "_"` e o prefixo nao e
vazio no dev. Custo alto, valor baixo perto do audit-access.

### Teste novo: tests/school-screen-action-payloads.test.ts (14 casos)
Motivo: use cases recebem `raw: unknown` e os schemas sao `z.strictObject`
- chave a mais/renomeada vira "Dados invalidos." em runtime que nada pegava.
Invariantes confirmadas contra os schemas reais:
- `capacity: 0` e **rejeitado** em `createTeamSchema`.
- Gratis e `priceCents: null` (+ `currency: null`); `priceCents: 0` e
  **rejeitado** por `updateTrainingProductDraftSchema`.
- `expectedVersion` = `updatedAt` em ISO (concorrencia otimista RNF-003).
- `grantProductAudienceSchema` aceita `email` (gestor nao sabe o id).
- `bulkAssignCoachSchema` **rejeita** athleteId repetido no lote.

### Bug de convites: corrigido pela raiz
Confirmado o que a memoria ja registrava: o painel montava link com
`InvitationLink.id`, mas a resolucao e por hash do token **bruto**, nunca
persistido (so `tokenHash`) -> 404 sempre. Correcao: exibir o token **uma
unica vez, na criacao** (unica hora em que existe em claro) e parar de
anunciar link recuperavel para convites antigos, irrecuperaveis por design.

## Testes E2E das telas de escola (specs 07-10) - 2026-09-16

### A infra de e2e JA EXISTIA - procurar antes de criar
`e2e/` + `playwright.config.ts` + `pnpm test:e2e`. `e2e/fixtures.ts` e a fonte
unica de escolas/professores/alunos; `e2e/helpers.ts` tem `ensureUser`,
`login`, `completeOnboarding`, `ensureCoachProfile`. **Nada de seed em banco**:
os specs 01-06 criam tudo pela UI e sao idempotentes por e-mail fixo.
Chromium nao vinha instalado: `npx playwright install chromium --with-deps`.

### Idempotencia: convergir para o estado, nao assumir o estado
Os tres bugs que escrevi (e que so apareceram rodando 2x seguidas):
1. `removerAlunoDaEscola` devolvia false quando o aluno **ja estava** fora.
   "Nada a fazer" e sucesso, nao falha.
2. A tela de atletas guarda **historico**: o mesmo atleta tem varias linhas
   ("Desligado" + ativa). `.first()` pegava a linha morta e o teste concluia
   errado. Alvo correto: a linha que tem o botao "Gerenciar"
   (`.filter({ has: ... })`), pois "Gerenciar" so e renderizado para ACTIVE.
3. Rodada que morre no meio deixa o atleta PENDING; o spec agora detecta isso
   (`temPedidoPendente`) e retoma na aprovacao.
**Rodar todo spec novo 2x seguidas** - a 1a passa por acidente.

### Skip silencioso esconde regressao
`test.skip(!cond)` no caminho principal fez o teste mais importante "passar"
sem exercitar nada. Trocar por `expect(cond, "mensagem").toBe(true)` nos
passos que sao a razao de ser do teste. Skip so para pre-condicao legitima.

### Seletores: preferir aria-label; cuidado com seletor amplo
- `"li, tr, div"` + `.last()` pegava div interna sem o botao. A lista de
  solicitacoes e `<ul><li>` - usar `page.locator("li")`.
- Clicar em link por texto pegava o item do **menu lateral** (`/turmas`) e
  voltava para a propria listagem. Navegar pelo `href` com id.
- Checkbox "Selecionar todos sem professor" nao some quando todos tem
  professor: fica `disabled`. Assertar `toBeDisabled()`, nao `toHaveCount(0)`.
- Onde ha `aria-label` (`Adicionar atleta`, `Adicionar professor`,
  `Buscar atleta`, `Professor`, `Link do convite`), usar `getByLabel`.
- Acoes destrutivas sao em 2-3 passos: "Gerenciar" -> "Desligar da escola" ->
  "Confirmar"; "Editar" -> "Arquivar turma" -> "Confirmar arquivamento".
  Arquivar turma fica **dentro do painel Editar da listagem**, nao no detalhe.

### O teste que prova o bug do convite
Captura o token no unico instante em que existe em claro (`getByLabel("Link do
convite")`) e abre num `browser.newContext()` **anonimo**. Token = 64 chars;
o id do convite tinha ~25 - por isso o link antigo dava 404. Um teste que so
olhasse a listagem passaria com o bug presente.

### Estado atual
51 testes e2e passando (31 pre-existentes + 20 novos), vitest 2598 intacto.
Erros de lint `no-explicit-any` em 04/05/06 sao **pre-existentes**.
Adicionado ao .gitignore: /test-results/, /e2e-report/, /playwright-report/.
